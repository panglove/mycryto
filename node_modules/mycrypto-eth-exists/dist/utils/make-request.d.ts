import { AxiosResponse } from 'axios';
export interface IHttpConfig {
    type: 'http';
    addr: string;
    port: number;
    timeout: number;
}
export declare function makeHttpRequest(config: IHttpConfig): Promise<AxiosResponse<any>>;
export interface IWSConfig {
    type: 'ws';
    addr: string;
    port: number;
    timeout: number;
}
export declare function makeWsRequest(config: IWSConfig): Promise<{}>;
export declare type RequestConfig = IWSConfig | IHttpConfig;
export declare function makeRequest(config: RequestConfig): Promise<{}>;
