import { IHttpConfig, IWSConfig, RequestConfig } from './utils/make-request';
export declare type SuccessConfig = RequestConfig & {
    success: true;
};
export declare type FailConfig = RequestConfig & {
    success: false;
    error: Error;
};
export interface IOptions {
    includeDefaults: boolean;
}
export declare function exists(configs?: RequestConfig[], opts?: IOptions): Promise<((IWSConfig & {
    success: true;
}) | (IHttpConfig & {
    success: true;
}) | (IWSConfig & {
    success: false;
    error: Error;
}) | (IHttpConfig & {
    success: false;
    error: Error;
}))[]>;
