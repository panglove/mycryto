import { nodeHash } from '../types';
export default function (domain: string, { parent, prefix }?: {
    parent?: string;
    prefix?: boolean;
}): nodeHash;
export declare function childhash(parent: nodeHash, label: string, options?: {
    prefix: boolean;
}): nodeHash;
